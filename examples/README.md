# Examples

Preview a minimal approval packet:

```bash
node ../dist/src/cli.js preview minimal-approval.yaml --format markdown
```

Write JSON evidence:

```bash
node ../dist/src/cli.js preview minimal-approval.yaml --format json --out /tmp/impact.json
```
